name: Release Chemotion CLI

on:
  push:
    branches:
      - wb_cli3_harivyasi

jobs:
  build-release-binary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v3
        with:
          go-version: "1.18"
          check-latest: true
      - name: Build Go for all OSes
        run: |
          cd  chemotion-cli
          go  mod          verify
          GOOS=linux   GOARCH=amd64 go  build -o chemotion
          GOOS=darwin  GOARCH=arm64 go  build -o chemotion.arm.x
          GOOS=darwin  GOARCH=amd64 go  build -o chemotion.amd.x
          GOOS=windows GOARCH=amd64 go  build -o chemotion.exe
          mv  chemotion ..
          mv  chemotion.arm.x ..
          mv  chemotion.amd.x ..
          mv  chemotion.exe ..
      - name: Release Binaries
        uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: "latest"
          prerelease: false
          files: |
            chemotion
            chemotion.arm.x
            chemotion.amd.x
            chemotion.exe
